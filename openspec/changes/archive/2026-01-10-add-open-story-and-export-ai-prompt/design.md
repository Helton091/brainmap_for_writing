## Context
节点的故事正文存放在外部 txt，应用只保存路径。为了让工作流更顺滑，需要在应用内直接打开该文件。另一方面，AI 创作需要把项目级设定（系统提示词/世界观）与当前节点的上下文要点（上游 memory blocks + 本节点文本）组织成结构清晰的提示词。

## Decisions
- Decision: 导出提示词由纯函数生成，UI 只负责选择节点与写文件
- Decision: 上游节点定义为沿有向边逆向可达的所有祖先节点（忽略折叠状态）
- Decision: 导出顺序按事件时间升序排列；无时间的节点排在最后并按 id 排序
